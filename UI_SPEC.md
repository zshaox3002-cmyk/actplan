# 友邦保险代理人活动管理小程序 UI 设计规范 v1.0

> 适用范围：微信小程序移动端，单人代理人使用场景
> 参考来源：PRD v1.1 + 用户提供 5 张参考图

---

## 一、设计语言定位

整体风格关键词：**专业、简洁、高效、可信赖**。以浅色为主基调，卡片式信息组织，数据可视化突出核心指标，操作路径短平快。避免过度装饰，拒绝消费类 App 的娱乐感，对标 Pipedrive、HubSpot 移动端的工具理性美学。

---

## 二、色彩系统

### 主色

| 用途 | 色值 | 说明 |
|------|------|------|
| 品牌主色 | `#1A6FD4` | 深蓝，用于主按钮、选中态、关键数字高亮 |
| 主色浅版 | `#E8F1FC` | 用于选中标签背景、卡片轻强调背景 |

### 辅助色

| 用途 | 色值 | 说明 |
|------|------|------|
| 成功 / 已完成 | `#27AE60` | 绿色，用于"已完成"状态标签、签单成交标记 |
| 警示 / 待定 | `#E74C3C` | 红色，用于❓待定问号图标、必填项提示 |
| 中性辅助 | `#F39C12` | 橙黄，用于"进行中"状态、图表第二色 |

### 苹果等级专属色

| 等级 | 色值 | 图标说明 |
|------|------|------|
| 红苹果 | `#E74C3C` | 红色苹果图标，优先级最高 |
| 青苹果 | `#27AE60` | 绿色苹果图标，跟进中 |
| 烂苹果 | `#6B7280` | 深灰色苹果图标，暂缓维护 |
| 待定 ❓ | `#E74C3C` | 红色问号，提示补全信息 |

### 中性色（背景 / 文字 / 分割线）

| 用途 | 色值 |
|------|------|
| 页面背景 | `#F5F6FA` |
| 卡片背景 | `#FFFFFF` |
| 主要文字 | `#1A1A2E` |
| 次要文字 | `#6B7280` |
| 辅助文字 / 占位符 | `#9CA3AF` |
| 分割线 | `#E5E7EB` |
| 置灰不可操作 | `#D1D5DB` |

### 图表双色系（对应拜访量 vs 计划量）

| 用途 | 色值 |
|------|------|
| 拜访量（实际） | `#1A6FD4` 蓝色 |
| 计划量 | `#D1D5DB` 浅灰色 |

---

## 三、字体规范

微信小程序统一使用系统默认字体（iOS 为 SF Pro / PingFang SC，Android 为 Roboto / Noto Sans SC），无需引入外部字体。

### 字号层级

| 层级 | 字号 | 字重 | 行高 | 用途 |
|------|------|------|------|------|
| 页面大标题 | 24px | Bold 700 | 32px | Dashboard 页面标题、详情页客户姓名 |
| 模块标题 | 18px | SemiBold 600 | 26px | 卡片区块标题（如"今日待拜访"） |
| 正文主要 | 15px | Regular 400 | 22px | 客户卡片姓名、列表主信息 |
| 正文次要 | 13px | Regular 400 | 20px | 卡片第二行信息（电话·拜访次数·日期） |
| 辅助说明 | 12px | Regular 400 | 18px | 表单字段 Label、占位提示文字 |
| 数据大字 | 32px | Bold 700 | 40px | Dashboard 核心指标数字（如"128"） |
| 数据标签 | 11px | Medium 500 | 16px | 指标卡下方说明文字（如"客户总量"） |

---

## 四、间距与栅格

以 **4px 为基础单位**，所有间距均为 4 的倍数。

| 场景 | 数值 |
|------|------|
| 页面左右边距 | 16px |
| 卡片内边距 | 16px |
| 卡片之间间距 | 12px |
| 区块之间间距 | 24px |
| 表单字段行间距 | 20px |
| 图标与文字间距 | 8px |
| 徽章内边距（横向） | 8px |
| 徽章内边距（纵向） | 4px |

---

## 五、圆角规范

| 元素 | 圆角值 | 说明 |
|------|------|------|
| 页面级卡片 | 16px | 客户卡片、计划卡片、Dashboard 指标卡 |
| 小型标签 / 徽章 | 20px（全圆角） | 跟进阶段徽章、苹果等级标签、优先级标签 |
| 输入框 | 10px | 搜索框、表单输入框 |
| 主按钮 | 12px | 主操作按钮（执行、保存、下一步） |
| 次要按钮 | 12px | 返回、取消类按钮 |
| 图表容器 | 16px | 饼图、柱状图外层容器卡片 |
| 底部 Tab 栏 | 0px | 无圆角，全宽铺满 |
| FAB 浮动按钮 | 50%（正圆） | 右下角新建按钮 |

---

## 六、核心组件规范

### 6.1 客户卡片

```
背景：#FFFFFF，圆角 16px，阴影：0 2px 8px rgba(0,0,0,0.06)
内边距：16px
第一行：苹果图标(20px) + 客户姓名(15px Bold) + 跟进阶段徽章(右对齐)
第二行：次要文字 13px，颜色 #6B7280，内容用「·」分隔
卡片间距：12px
```

### 6.2 跟进阶段徽章

| 阶段 | 背景色 | 文字色 |
|------|------|------|
| 需求沟通 | `#E8F1FC` | `#1A6FD4` |
| 已成交 | `#DCFCE7` | `#27AE60` |
| 已拒绝 | `#FEE2E2` | `#E74C3C` |

字号 12px，字重 Medium 500，圆角 20px，横向内边距 8px，纵向 4px。

### 6.3 主操作按钮

```
高度：48px，圆角：12px
背景：#1A6FD4，文字：#FFFFFF，字号 15px，字重 SemiBold 600
按下态：背景加深至 #155BB5
禁用态：背景 #D1D5DB，文字 #9CA3AF
```

### 6.4 次要按钮 / 描边按钮

```
高度：48px，圆角：12px
背景：#FFFFFF，边框：1px solid #E5E7EB，文字：#1A1A2E
用于"返回""取消"等操作
```

### 6.5 标签式单选（表单选项）

参考图四的选项标签样式：
```
未选中：背景 #FFFFFF，边框 1px solid #E5E7EB，文字 #6B7280
选中：背景 #1A6FD4，边框无，文字 #FFFFFF
圆角：20px，高度：36px，横向内边距：16px
```

用于：拜访方式（面对面/电话/微信）、单选字段。

### 6.6 FAB 浮动操作按钮

```
尺寸：56px × 56px，正圆
背景：#1A6FD4，图标：白色 + 号，24px
位置：右下角，距右 20px，距底部 Tab 栏上方 16px
阴影：0 4px 16px rgba(26,111,212,0.35)
```

### 6.7 表单输入框

```
高度：48px，圆角：10px
背景：#F5F6FA，边框：无（默认）/ 1px solid #1A6FD4（聚焦态）
字号：15px，文字色：#1A1A2E，占位符色：#9CA3AF
Label：12px，颜色 #6B7280，位于输入框上方，间距 6px
```

### 6.8 底部导航 Tab 栏

参考图一底部 Tab 结构：
```
高度：56px + 底部安全区
背景：#FFFFFF，顶部分割线：1px solid #E5E7EB
4个入口：概览 / 客户 / 计划 / 记录
选中态：图标 + 文字均用主色 #1A6FD4
未选中：图标 + 文字均用 #9CA3AF
文字字号：11px
中间 FAB 凸起按钮独立于 Tab 栏之上
```

### 6.9 周视图日历

参考图一 Schedule 页：
```
背景：#FFFFFF，圆角 16px 卡片包裹
日期列：7列等宽，每格高度 56px
日期数字：15px，颜色 #1A1A2E
选中日期：背景圆圈 #1A6FD4，数字 #FFFFFF
今日日期（未选中）：数字 #1A6FD4，无背景
有计划标记：日期数字下方 4px 处，4px 圆点，颜色 #1A6FD4
```

### 6.10 Dashboard 指标卡（2×2 网格）

参考图二的 2×2 小数据卡片：
```
每张卡片：背景 #FFFFFF，圆角 16px，内边距 16px
数字：32px Bold，颜色 #1A1A2E
标签：12px Regular，颜色 #6B7280
卡片间距：12px
网格：两列等宽，左右边距 16px
```

---

## 七、图标风格

统一使用**线性图标（Outline 风格）**，描边粗细 1.5px，圆角笔触。推荐使用 [Lucide Icons](https://lucide.dev) 或微信小程序自带图标库，与整体简洁风格一致。图标尺寸：

| 场景 | 尺寸 |
|------|------|
| Tab 栏图标 | 24px |
| 卡片内功能图标 | 20px |
| 表单字段前缀图标 | 18px |
| 苹果等级图标（emoji） | 20px |

---

## 八、多步骤表单进度条

参考图四、图五底部步骤指示器，用于新建拜访记录的流程：

```
步骤节点：圆形图标，完成态绿色对勾，当前态主色填充，未到达态灰色
步骤间连线：1px，完成段 #27AE60，未完成段 #E5E7EB
文字：12px，颜色与节点状态对应
位置：底部操作区上方，与 Back / Next 按钮组合
```

---

## 九、整体设计原则

**信息层级不超过三层**，每个页面只有一个最主要操作。卡片阴影保持克制（`0 2px 8px rgba(0,0,0,0.06)`），不用重阴影堆叠。状态反馈必须即时可见（成功 Toast、加载骨架屏、空状态插图）。所有可点击元素最小触控区域不小于 44×44px。置灰字段在编辑态中视觉上明显区别于可编辑字段，避免用户误操作。

---

## 十、CSS 变量速查表

```css
:root {
  /* 主色 */
  --color-primary: #1A6FD4;
  --color-primary-light: #E8F1FC;
  --color-primary-dark: #155BB5;

  /* 辅助色 */
  --color-success: #27AE60;
  --color-warning: #F39C12;
  --color-danger: #E74C3C;

  /* 中性色 */
  --color-bg-page: #F5F6FA;
  --color-bg-card: #FFFFFF;
  --color-text-primary: #1A1A2E;
  --color-text-secondary: #6B7280;
  --color-text-placeholder: #9CA3AF;
  --color-border: #E5E7EB;
  --color-disabled: #D1D5DB;

  /* 苹果等级 */
  --color-apple-red: #E74C3C;
  --color-apple-green: #27AE60;
  --color-apple-gray: #6B7280;

  /* 图表 */
  --color-chart-actual: #1A6FD4;
  --color-chart-plan: #D1D5DB;

  /* 间距 */
  --spacing-page: 16px;
  --spacing-card: 16px;
  --spacing-card-gap: 12px;
  --spacing-section: 24px;
  --spacing-form-row: 20px;
  --spacing-icon-text: 8px;

  /* 圆角 */
  --radius-card: 16px;
  --radius-badge: 20px;
  --radius-input: 10px;
  --radius-button: 12px;

  /* 阴影 */
  --shadow-card: 0 2px 8px rgba(0, 0, 0, 0.06);
  --shadow-fab: 0 4px 16px rgba(26, 111, 212, 0.35);

  /* 字体 */
  --font-size-title: 24px;
  --font-size-module: 18px;
  --font-size-body: 15px;
  --font-size-secondary: 13px;
  --font-size-helper: 12px;
  --font-size-data: 32px;
  --font-size-data-label: 11px;
}
```
